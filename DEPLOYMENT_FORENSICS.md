# Deployment Forensics - yait Wave Reveal Edge

## Deployment Details

**Date:** 2026-06-12
**Service:** party-app (Railway, environment: production)

## Commits Being Deployed

- wave-reveal-edge plan
- wave-reveal-edge implementation

## Changes Deployed

1. The slanted reveal boundary becomes a sine wave: one crest and one trough
   deviating 50px from the slant line at the 1280px reference viewport (scaling
   proportionally elsewhere). The polygon clip is replaced by an inline SVG
   clipPath (clipPathUnits=objectBoundingBox, id yait-wave-clip) whose path is
   generated by buildWaveEdgePath in heroScene.ts (48 samples along the slant).
   The clip stays static; only translateX animates — compositor guarantee
   unchanged. The unused --headline-slant custom property was removed with the
   polygon rule.

## Cutover Sentinel

GET https://yait.social/home contains "yait-wave-clip" (verified absent in the
BEFORE check; present in HTML and CSS locally).

## Pre-Deploy Validation

- 88 unit/canary/integration green (6 new wave-generator tests: closed region,
  exact slant anchors, monotonic descent, crest and trough at the amplitude,
  sampling density, reference round-trip; canary swapped to the url clip; new
  integration check that the served HTML ships the generated path)
- 9 e2e green (reworked angle test parses the rendered wave path: slant/height
  ratio in the 45-degree band, max deviation +50px and min -50px within 15px
  tolerance at 1280; compositor guard still transform/opacity only)
- Frames reviewed: the boundary through the lockup undulates while sweeping

## Earlier deployments today

- yait Home Landing: cutover 42s on the /home 404-to-200 sentinel, fully validated.
  Includes db-pool-resilience (idle pg connection loss no longer crashes the
  process). Known issue found after: the sail entrance was visually inert
  (dock-settle backwards fill override).
- yait S-Curve Sail-In: cutover 43s; fixed the inert entrance and shipped the S
  course. Superseded by three-beat-sail.
- yait Three-Beat Sail: cutover 42s; split travel from weave for three felt beats.
- yait Wake Reveal: cutover 32s; boat-synced wipe replaced word timers.
- yait Hull-Locked Reveal (2026-06-11): cutover 53s; bow-locked edge.
- yait Stern-Locked Reveal: cutover 42s; reveal edge moved to the stern (edge and
  stern both 115px at beat 1 on prod).
- yait Staggered Headline: cutover 53s; left-aligned lockup, prod indent 100px.
- yait Slanted Reveal Edge: cutover 42s on the clip-path:polygon sentinel; prod
  probe measured 285.12px slant vs 287px height = 45 degrees.

## Final Status Assessment

**Deployment Status:** PENDING
